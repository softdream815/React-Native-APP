query getCity{
  getCity{
     count,
     currentPage,
     message,
     nextPage,
     prevPage,
     success,
     totalPages,
     result{
       cityId,
       cityName,
       createdBy,
       createdDate,
       isActive,
       modifiedBy,
       modifiedDate,
       provinceId
     }
   }
 }